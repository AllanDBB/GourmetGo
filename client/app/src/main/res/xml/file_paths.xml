<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Directorio interno de archivos de la aplicación -->
    <files-path name="files" path="." />

    <!-- Directorio de caché interno de la aplicación -->
    <cache-path name="cache" path="." />

    <!-- Directorio externo de archivos de la aplicación -->
    <external-files-path name="external_files" path="." />

    <!-- Directorio de caché externo de la aplicación -->
    <external-cache-path name="external_cache" path="." />

    <!-- Directorio específico para imágenes -->
    <files-path name="images" path="images/" />
</paths>